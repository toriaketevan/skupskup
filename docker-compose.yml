services:
  mysql:
    image: mysql:8.0
    container_name: skupskup-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: skupskup123
      MYSQL_DATABASE: skupskup
      MYSQL_USER: skupskup
      MYSQL_PASSWORD: skupskup123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./backend/init.sql:/docker-entrypoint-initdb.d/init.sql

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: skupskup-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: mysql
      PMA_USER: skupskup
      PMA_PASSWORD: skupskup123
    ports:
      - "8080:80"
    depends_on:
      - mysql

volumes:
  mysql_data:
